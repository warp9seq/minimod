# Notes


## Information about the implementations

- From >=v0.5.0, we print a warning if a certain context is untested.  For versions before this, only m[CG] for DNA (genome) and a[A] for transcriptome were tested.
-


## Modkit consistency
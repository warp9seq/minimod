name: C/C++ CI

on:
  push:
    branches: [ '*' ]
  pull_request:
    branches: [ '*' ]

jobs:
  ubuntu_20:
    name: Ubuntu 22
    runs-on: ubuntu-22.04
    steps:
    - uses: actions/checkout@v2
    - name: install htslib
      run: ./scripts/install-hts.sh
    - name: test
      run: make test
    - name: memtest
      run: make memtest
